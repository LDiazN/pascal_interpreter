program SpecialExpressions;

var 
    a: real;
    cosine :real;
    sine :real;
    square :real;
    exponent :real;
    lnFunc :real;


begin
    cosine := cos(0.0);
    sine := sin(0.0);
    square := sqrt(4.0);
    exponent := exp(0.0);
    lnFunc := ln(1.0);
    writeln(cosine);
    writeln(sine);
    writeln(square);
    writeln(exponent);
    writeln(lnFunc);
end.